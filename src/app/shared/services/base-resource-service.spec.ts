import { BaseResourceService } from './base-resource-service';

describe('BaseResourceService', () => {
  it('should create an instance', () => {
    expect(new BaseResourceService()).toBeTruthy();
  });
});
